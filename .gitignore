node_modules/
coverage/

# Служебные бэкапы, которые app.js/index.html делал перед рискованными
# правками (разбиение на модули, крупные правки) — не нужны в репозитории.
_backup_before_*/

# Локальные настройки Claude Code — не для публикации.
.claude/
